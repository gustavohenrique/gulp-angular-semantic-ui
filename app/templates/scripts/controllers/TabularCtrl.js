;(function (angular) {
    'use strict';

    var app = angular.module('controllers.Tabular', []);

    
    app.controller('TabularCtrl', ['$scope',

        function ($scope) {
        }
    ]);

})(angular);